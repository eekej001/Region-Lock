<h1>Customers</h1>

<ul>
  
  <table>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
    </tr>
    <% @customers.each do |customer| %>
    <tr>
    <td><%= customer.first_name %></td>
    <td><%= customer.last_name %></td>
    <td><%= customer.email %></td>
    </tr>
    <% end %>
  </table>

</ul>